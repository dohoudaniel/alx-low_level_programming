#include <stdio.h>

/*
 * main - Prints the program name, folloewd by a new line
 *
 * Return: Always returns 0
 */

int main(int argc __attribute__((unused)), char *argv[])
{
	printf("%s\n", argv[0]);

	return (0);
}
