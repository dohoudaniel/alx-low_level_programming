#include <stdio.h>
/*
 * main - Entry point
 *
 * Return: always return 0
 *
 */
int main(void) 
/**
 * function_main - lists alphabets a to z
 */
{
	char a;

	for (a = 'a' ; a <= 'z' ; a++)
	{
		putchar(a);
	}
	putchar('\n');
	return (0);
}
