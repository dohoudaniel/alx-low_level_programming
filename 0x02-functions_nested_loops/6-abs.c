#include "main.h"

/**
 * _abs - main function
 *
 * @n: An Integer to make absolute
 *
 * Return: int or int times negative.
 *
 */

int _abs(int n)
{
	if (n > 0)
		return (n);
	else
		return (n * -1);
}
